Implements the depth first search algorithm on a given graph G with vertices V and edges E

Procedure:
1. For each node U in V, chose U
2. For each of its neighbors N, if the neighbor hasn't been visited, visit it.
  3. Recursively visit all of Ns neighbors
