Implements the QuickSort algorithm for sorting an Array A of size N in O(N log N) time on average.

Procedure:
1. Randomly chose a pivot element. Swap it with the first element
2. Partition the array
3. Recurse on the left and right side of the pivot

Partition:
1. Put all elements smaller than the pivot on its left and greater on the right
