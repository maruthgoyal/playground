This program implements the Counting Sort algorithm for sorting integers. Sorts an array A of size N with max M in O(M) time

Procedure:
1. Count the frequency of each element
2. Create new list
3. Add items to list starting from smallest to largest value according to their frequency
