Implements the insertion sort algorithm for sorting an Array A of size N in O(N^2) time.

Procedure:

1. for each element in A
      While the element before it is larger than it, swap the element before it with the current element
